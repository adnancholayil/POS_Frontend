# Live backend on Render
VITE_API_URL=https://pos-backend-ocsh.onrender.com/api/v1

# Set to 'true' to re-enable the local mock DB for offline development
VITE_USE_MOCK=false

# Base URL used by the WebSocket hook (no /api/v1 suffix)
VITE_SOCKET_URL=https://pos-backend-ocsh.onrender.com
